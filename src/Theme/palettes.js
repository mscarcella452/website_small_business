const palette = {
  background: {
    // primary:
    // secondary:

    fontColor: {
      // primary:
      // secondary:
      // white:
      // black:
    },
  },
  primary: {
    // light: will be calculated from palette.primary.main,
    // main:
    // fontColor:
  },
  secondary: {
    // light: will be calculated from palette.primary.main,
    // main:
    // fontColor:
  },
  info: {
    // main:
    // fontColor:
  },
};

export { palette };
